# Generate an sbom for the adservice

name: Adservice SBOM
'on':
  workflow_dispatch: {}
  push: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: '0'
      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v0.9.14
        with:
          versionSpec: 5.x
      - id: determine_version
        name: Determine Version
        uses: gittools/actions/gitversion/execute@v0.9.14
        with:
          additionalArguments: /overrideconfig mode=Mainline
      - name: Install Octopus Deploy CLI
        uses: OctopusDeploy/install-octopus-cli-action@v1
        with:
          version: latest
      - name: Build SBOM
        run: |        
          chmod +x gradlew
          ./gradlew cyclonedxBom
        working-directory: src/adservice
      - name: Build SBOM package
        run: >
          octo pack
          --id adservice-sbom
          --version ${{ steps.determine_version.outputs.semVer }}
          --format zip
          --include **/bom.xml
        shell: bash
        working-directory: src/adservice
      - run: >
          ./mvnw deploy:deploy-file
          -Dfile=adservice-sbom.${{ steps.determine_version.outputs.semVer }}
          -Durl="https://${{ github.actor }}:${{ secrets.GH_PACKAGES_PAT }}@maven.pkg.github.com/${{ github.repository }}"
          -DgroupId=microservices-demo
          -DartifactId=adservice-sbom
          -Dversion=${{ steps.determine_version.outputs.semVer }}
          -Dpackaging=zip
        shell: bash
        working-directory: src/adservice